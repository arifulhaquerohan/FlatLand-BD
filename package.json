{
  "name": "flatlandbd",
  "version": "2.0.0",
  "private": true,
  "description": "FlatLand BD - flats and interior studios in Bangladesh. The front end is hand written CSS and vanilla JS with no build step, so there is nothing to compile before deploying.",
  "scripts": {
    "note": "echo 'No build step. Edit frontend/static/css/*.css and frontend/static/js/*.js directly, then hard refresh.'"
  }
}
